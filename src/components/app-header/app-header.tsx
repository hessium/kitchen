import { Icon } from '@/shared/ui/icon/icon';

export const AppHeader = () => (
  <header className='h-header span:font-bold flex-center gap-6 bg-zinc-600'>
    <span>HeadSpan</span>

    <Icon name='common/test' />
  </header>
);
